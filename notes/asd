'The concept is following: My program is reading file which contains people, the format of the file is non-known, for now i need to support csv and xml (need to assume that more extensions will be supported in the future) and save this peoples to database. There's one assumption; File can be large, so reading full file into memory is forbidden, it will be solved by reading specific count of bytes, mapping read peoples to objects and then saved them into DB.  Let's define Person class: class Person { String name int age } Fixed buffer would read few people but when he start to read the last one in actual buffer he will end for example after 'name' and the full customer will not be read. If my program'
